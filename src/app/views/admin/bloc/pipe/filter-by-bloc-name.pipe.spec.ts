import { FilterByBlocNamePipe } from './filter-by-bloc-name.pipe';

describe('FilterByBlocNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByBlocNamePipe();
    expect(pipe).toBeTruthy();
  });
});
